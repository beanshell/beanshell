<section>
<name>Modes of using BeanShell</name>

There are currently five basic modes of use for BeanShell:

<ul>
<li>Standalone scripts</li>
<li>Embedded in your application</li>
<li>Remote server mode</li>
<li>Servlet mode for in-container scripts</li>
<li>Applet mode</li>
</ul>

<h2>Standalone Scripts</h2>

<img src="scriptmode.gif"/>
<br CLEAR="ALL"/>


<h2>Embedding BeanShell in Your Application</h2>

<img src="embeddedmode.gif"/>
<br CLEAR="ALL"/>
<example>
import bsh.Interpreter;
...

Interpreter i = new Interpreter();
i.set("foo", 5);
i.eval("bar = foo*10");
System.out.println("bar = "+i.get("bar") );
</example>

Note: The trailing ";" semi-colon in the string containing the bsh statement 
to be evaluated is optional in this mode (bsh simply always adds one).
<p/>

<h2>Remote Server Mode</h2>

<img src="remotemode.gif"/>
<br CLEAR="ALL"/>

<h2>The BeanShell Servlet</h2>

<img src="servletmode.gif"/>
<br CLEAR="ALL"/>

<h2>The BeanShell Applet</h2>

<img src="appletmode.gif"/>
<br CLEAR="ALL"/>


</section>
