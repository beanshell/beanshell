
bsh.help.server = "usage: server(int port)";

server(int port ) {
	new Thread( new bsh.util.Httpd( port ) ).start();
	print("Httpd started on port: "+port);
	new Thread( new bsh.util.Sessiond( global.namespace, port+1 ) ).start();
	print("Sessiond started on port: "+ (port+1));
}
