
bsh.help.addClassPath= "usage: addClassPath( string | URL )";
import java.net.URL;

addClassPath( path ) {
	URL url;
	if ( path instanceof URL )
		url = path;
	else
		url = pathToFile( path ).toURL();

	bsh.BshClassManager.getClassManager().addClassPath( url, this.interpreter );
}

