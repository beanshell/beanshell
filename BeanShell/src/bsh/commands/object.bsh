/**
	Returns a new bsh object which you can use to hold data items.  
	e.g. 
	<p>
	<pre>
	  myStuff = object();
	  myStuff.foo = 42;
	  myStuff.bar = "blah";
	</pre>

	@method This object()
*/

bsh.help.object = "usage: object()";

object() { 
	return this; 
}

