#!/bin/java bsh.Interpreter

source("TestHarness.bsh");

if ( bsh.Capabilities.haveProxyMechanism() ) {
	r=new Runnable() {
		run() {
			flag();
		}
	};

	r.run();

	assert( flag() == 1 );
}
	else
		warning("No proxy mechanism.");

complete();

