
threadBody = new Runnable() {
        run() {
                try {
                        Thread.yield();
                        int val = 1/0;
                } catch (Exception e) {
                    System.out.println(e.getMessage());
                }

				// somewhat empirical, but shouldn't change.
				// assert(this.callstack.depth() == 2 );
        }
};

numThreads = 9;

for (int i=0; i<numThreads; i++) {
        thread = new Thread(threadBody);
        thread.start();
}

